# Scripts

## 中文
- `./scripts/verify`
  - 用途：执行本仓库最小验证（shell 语法、关键文件存在、`apis.json.example` 可解析）
  - 预期：输出 `[verify] OK` 且返回码 `0`

## English
- `./scripts/verify`
  - Purpose: run minimal repository checks (shell syntax, required files, `apis.json.example` JSON parse)
  - Expected: prints `[verify] OK` and exits with code `0`
